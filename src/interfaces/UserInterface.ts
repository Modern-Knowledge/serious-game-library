/**
 * Interface for all models that contain a user id
 *
 * getUserId: returns the userId of the model
 */
export interface UserInterface {
    /**
     * returns user id
     */
    getUserId(): number;
}
